# ============================================================================
# GW2 WvW 日志系统 - Python 依赖清单
# Python 版本要求: >=3.12, 推荐 3.13
# 安装方式: pip install -r requirements.txt
# ============================================================================

# ---------------------- FastAPI Web 框架 ----------------------
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.20      # FastAPI 文件上传支持

# ---------------------- 数据库 ----------------------
sqlalchemy>=2.0.0
pymysql>=1.1.0                # MySQL 驱动 (必须)
cryptography>=44.0.0          # PyMySQL 的加密依赖 (必须)
greenlet>=3.0.0               # SQLAlchemy 异步支持

# ---------------------- 数据验证与配置 ----------------------
pydantic>=2.10.0
pydantic-settings>=2.7.0

# ---------------------- 认证与安全 ----------------------
pyjwt>=2.10.0                 # JWT Token
passlib[bcrypt]>=1.7.4        # 密码哈希

# ---------------------- HTTP 客户端 ----------------------
httpx>=0.28.0                 # 异步 HTTP (AI API 调用)
requests>=2.32.0              # 同步 HTTP (dps.report 等)

# ---------------------- 任务调度 ----------------------
apscheduler>=3.11.0

# ---------------------- AI / LLM ----------------------
openai>=1.60.0                # OpenAI SDK (兼容 DeepSeek/Qwen)

# ---------------------- 开发/测试依赖 (可选) ----------------------
pytest>=8.0.0
pytest-cov>=6.0.0
pytest-asyncio>=0.25.0
